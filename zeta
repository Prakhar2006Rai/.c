#include <stdio.h>
int main(){
    int a[2][3],i,j,sum=0;
    printf("enter 6 elements:\n");
    for(i=0;i<2;i++)
    for(j=0;j<3;j++){
        scanf("%d",&a[i][j]);
        sum +=a[i][j];
    }
    printf("sum of all elements = %d\n",sum);
    return 0;
}
